\documentclass[12pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{hyperref}

\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    breaklines=true,
    keywordstyle=\color{blue},
    commentstyle=\color{gray},
    stringstyle=\color{red},
    showstringspaces=false
}

\title{Machine Learning Homework 1 Report}
\author{}
\date{}

\begin{document}

\maketitle

\section*{Student Information}
\begin{tabular}{ll}
    Name: Adilhan Çetin&  \\
    Student Number: 2580421&
\end{tabular}

\vspace{1cm}

\section*{Question 1}

\subsection*{a)}
The model achieved the following performance metrics on the test set:
\begin{itemize}
    \item \textbf{Accuracy:} 0.9362
    \item \textbf{Precision (Macro):} 0.8719
    \item \textbf{Recall (Macro):} 0.8860
    \item \textbf{F1-Score (Macro):} 0.8787
\end{itemize}

The model demonstrates strong overall predictive capabilities, achieving a high accuracy of roughly 93.6\%. While the accuracy is excellent, the precision, recall, and F1-scores (around 87-88\%) offer a more nuanced view of the model's performance. Because the \textit{Macro} averaging method was used, we can confidently say the model generalized well across all class labels—meaning it successfully identified the minority classes rather than strictly predicting the majority class. The minor gap between accuracy and the macro metrics suggests a slight imbalance in the dataset, but an F1 score of 0.8787 indicates a very healthy balance between false positives and false negatives.

\subsection*{b)}
For this problem, I implemented the ID3 Decision Tree algorithm entirely from scratch, adapting the pseudocode provided in the lecture slides to handle categorical features. My setup relies on a recursive \texttt{id3\_training\_loop} that first checks for base stopping cases (uniform labels, empty dataset, or no remaining attributes) to assign majority-vote leaf nodes. If the base cases are not met, it calculates the Information Gain for all available features using a custom entropy function, selects the feature that maximizes this gain, and dynamically generates multi-way branches for every possible value of that feature by calling itself recursively. After the tree is built, predictions are made by tracing downward from the root, and I calculate the macro accuracy, precision, recall, and F1 metrics manually to evaluate the multi-class performance.

\subsection*{c)}

Since the full tree generates 238 distinct rules, I added an \texttt{extract\_rules} function in the code to trace and retrieve every path programmatically. Below is a concise excerpt of rules generated directly from our learned ID3 tree, showing examples for \texttt{unacc}, \texttt{acc}, and \texttt{vgood} classes:

\begin{align*}
\text{Prediction}(\text{unacc}) \iff & (\text{safety} = \text{low}) \lor \\
& (\text{safety} = \text{med} \land \text{persons} = 2) \lor \\
& (\text{safety} = \text{high} \land \text{persons} = 2) \\
\\
\text{Prediction}(\text{acc}) \iff & (\text{safety} = \text{high} \land \text{persons} = 4 \land \text{buying} = \text{low} \land \text{maint} = \text{vhigh}) \lor \\
& (\text{safety} = \text{med} \land \text{persons} = 4 \land \text{buying} = \text{low} \land \text{maint} = \text{vhigh} \land \text{lug\_boot} = \text{big}) \\
\\
\text{Prediction}(\text{vgood}) \iff & (\text{safety} = \text{high} \land \text{persons} = 4 \land \text{buying} = \text{low} \land \text{maint} = \text{low} \land \text{lug\_boot} = \text{big})
\end{align*}

\vspace{1cm}

\section*{Question 2}

\subsection*{a)}
I trained two separate decision tree models using different splitting criteria. Here is how they performed on the test set:

\begin{itemize}
    \item \textbf{Gini Criterion:} Accuracy: 0.8917 | Precision: 0.9089 | Recall: 0.9088 | F1-Score: 0.9088
    \item \textbf{Entropy Criterion:} Accuracy: 0.8975 | Precision: 0.9102 | Recall: 0.9103 | F1-Score: 0.9102
\end{itemize}

Both models handled the test data very well, scoring around 89--91\% across the board. The Entropy model slightly outperformed the Gini model, pulling ahead by about 0.6\% in accuracy. This makes sense, as Entropy tends to naturally produce slightly more balanced trees. More importantly, the consistently high macro F1-scores tell us that both models successfully learned to identify the minority bean classes rather than just taking the easy route of predicting the most common ones.

\subsection*{b)}
For this task, I used the \texttt{scikit-learn} library to build the decision trees and the \texttt{ucimlrepo} package to cleanly pull in the Dry Bean Dataset. 

First, I split the data into 80\% training and 20\% test sets. To make sure no bean class was accidentally underrepresented in either set, I used a stratified split (\texttt{stratify=y}). I also set a fixed \texttt{random\_state=42} everywhere as stated in the homework instructions. I have not used in hyperparameter tuning. Finally, I evaluated them using \texttt{scikit-learn}'s built-in metrics.

\vspace{1cm}

\section*{Question 3}

\subsection*{a)}
The \texttt{sklearn.tree.DecisionTreeClassifier} uses an optimized version of the CART (Classification and Regression Trees) algorithm. The largest difference from our ID3 implementation is that CART handles continuous, numeric features natively by dividing data at optimal threshold values ($x \le \text{threshold}$) to create strictly binary splits, rather than making multi-way branches for every category. Furthermore, while ID3 relies purely on entropy for classification tasks, CART supports both classification and regression, and gives you the choice to split based on either Gini Impurity (its default) or Entropy.

\subsection*{c)}

The best approach would be to use a algorithm similar to binary search. Even tough we have a continous numeric feature, we can create finite number of splits by finding the optimal threshold values. We can sort the data based on the feature we want to split on and then evaluate potential splits at each unique value of that feature. This way, we can effectively handle continuous features while still maintaining the decision tree structure.

\end{document}

